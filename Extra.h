#include <fstream>
#include <vector>
#include <iostream>
#include <string>
using namespace std;
class Extra
{
private:
public:
	static vector<char> readFile(int test_board);
	static vector<int> counter(int number);
};

